name: W512-ubuntu
on:
  schedule:
    - cron: '*/10 * * * *' # Run every day at midnight
jobs:
  download-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: |
          wget https://raw.githubusercontent.com/MoeClub/OneList/master/OneDriveUploader/amd64/linux/OneDriveUploader -P /usr/local/bin/
          wget https://jp678022.alwaysdata.net/cad512/main/cad512.json
          chmod +x /usr/local/bin/OneDriveUploader
          mkdir mainn
          cp *.* mainn
          OneDriveUploader -c cad512.json -s "mainn"
          wget -r -np -nd -A json https://jp678022.alwaysdata.net/cad512/main/
          wget -r -np -nd https://jp678022.alwaysdata.net/cad512/mainn/
      - name: Run file
        run: |
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230121/*
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230120/*
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230119/*
          cp www/230121/*.* mainn
          cp www/230120/*.* mainn
          cp www/230119/*.* mainn
          cp *.* mainn
          OneDriveUploader -c cad512.json -s "mainn"
          
          
          
          

    
