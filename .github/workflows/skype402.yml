name: skype402-ubuntu
on:
  schedule:
    - cron: '20 * * * *' # Run every day at midnight
jobs:
  download-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: |
          wget https://raw.githubusercontent.com/MoeClub/OneList/master/OneDriveUploader/amd64/linux/OneDriveUploader -P /usr/local/bin/
          wget https://go--fff0090.repl.co/skype402/main/skype402.json
          chmod +x /usr/local/bin/OneDriveUploader
          wget -r -np -nd -A json https://go--fff0090.repl.co/skype402/main/
          mkdir mainn
          cp *.* mainn
          OneDriveUploader -c skype402.json -s "mainn"
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230121/*
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230120/*
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230119/*
          cp www/230121/*.* mainn
          cp www/230120/*.* mainn
          cp www/230119/*.* mainn
          cp *.* mainn
          wget -O skype402.json https://go--fff0090.repl.co/skype402/main/skype402.json
          OneDriveUploader -c skype402.json -s "mainn"
          wget -r -np -nd  -A json https://go--fff0090.repl.co/skype402/mainn/
          
          

    
